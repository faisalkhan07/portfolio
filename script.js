// This page is intentionally simple — the logo's hover animation and the
// blinking cursor are both done in style.css, no JavaScript required.
// Keeping this file here so it's easy to add interactivity later
// (e.g. a click counter, a theme toggle, a small easter egg).
